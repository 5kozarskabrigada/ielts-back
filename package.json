{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "normalize:question-types": "node scripts/normalize-question-types.js",
    "normalize:question-types:apply": "node scripts/normalize-question-types.js --apply",
    "cleanup:duplicate-exams": "node scripts/cleanup-duplicate-exams.js",
    "cleanup:duplicate-exams:apply": "node scripts/cleanup-duplicate-exams.js --apply",
    "repair:reading-from-duplicate": "node scripts/repair-exam-reading-from-duplicate.js",
    "audit:question-types": "node scripts/audit-question-type-persistence.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.19.0",
    "jsonwebtoken": "^9.0.0",
    "multer": "^2.0.2",
    "pg": "^8.18.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
